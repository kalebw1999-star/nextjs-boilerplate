import { NextResponse } from "next/server";
import { getCurrentUser } from "../../../auth";
import { ensureSchema, getDb, isAdminUsername } from "../../../db";

const REQUEST_COOLDOWN_MS = 60 * 1000;
const MAX_BODY = 2000;
const FLAG_PATTERNS: [RegExp, string][] = [[/\b(?:n[i1]gg(?:er|a)|f[a4]gg?[o0]t|tr[a4]nn[y1]|k[i1]ke)\b/i,"possible hateful slur"],[/\b(?:kill yourself|kys|go die|rape|molest|child porn|cp)\b/i,"possible violent or sexual abuse content"],[/\b(?:fuck|fucked|fucking|shit|bitch|cunt|cock|dick|pussy|whore|slut|cum|semen|nudes?|send nudes?)\b/i,"possible sexual or explicit language"]];
type Person={id:string;username:string;status:"none"|"waiting"|"team";teamId:string|null;isRecruiter:boolean};
function pair(a:string,b:string){return a<b?[a,b] as const:[b,a] as const}
function isAdmin(username:string){return isAdminUsername(username)}
function moderateMessage(message:string){const hit=FLAG_PATTERNS.find(([pattern])=>pattern.test(message));return hit?{status:"flagged",reason:hit[1]}:{status:"clear",reason:null}}
async function getPerson(db:ReturnType<typeof getDb>,userId:string):Promise<Person|null>{const rows=await db`SELECT u.id,u.username,COALESCE(rs.status,'none') AS status,rs.team_id,EXISTS(SELECT 1 FROM team_memberships tm WHERE tm.user_id=u.id AND tm.role='recruiter') AS is_recruiter FROM users u LEFT JOIN recruitment_status rs ON rs.user_id=u.id WHERE u.id=${userId}`;if(!rows.length)return null;return{id:String(rows[0].id),username:String(rows[0].username),status:rows[0].status,teamId:rows[0].team_id??null,isRecruiter:Boolean(rows[0].is_recruiter)||isAdmin(String(rows[0].username))}}
async function hasTest(db:ReturnType<typeof getDb>,userId:string){const rows=await db`SELECT EXISTS(SELECT 1 FROM attempts WHERE user_id=${userId}) AS taken`;return Boolean(rows[0]?.taken)}
function canCommunicate(sender:Person,target:Person){if(sender.status==='waiting')return target.status==='waiting';if(sender.status==='team')return(target.status==='team'&&target.teamId===sender.teamId)||(target.isRecruiter&&target.teamId===sender.teamId);return true}
function shouldRequireApproval(sender:Person,target:Person){return target.isRecruiter&&!sender.isRecruiter}
async function getThread(db:ReturnType<typeof getDb>,firstId:string,secondId:string){const[userA,userB]=pair(firstId,secondId);const rows=await db`SELECT id,user_a,user_b,approval_status,requested_at,approved_at,last_request_at FROM dm_threads WHERE user_a=${userA} AND user_b=${userB} LIMIT 1`;return rows[0]??null}

export async function GET(request:Request){try{await ensureSchema();const user=await getCurrentUser();if(!user)return NextResponse.json({error:"Not authenticated."},{status:401});const db=getDb();const me=await getPerson(db,user.id);if(!me)return NextResponse.json({error:"Profile not found."},{status:404});if(!me.isRecruiter&&!(await hasTest(db,me.id)))return NextResponse.json({error:"Take the assessment at least once to unlock messages."},{status:403});const url=new URL(request.url);const targetId=url.searchParams.get("userId");const requests=url.searchParams.get("requests")==="1";const directory=url.searchParams.get("directory")==="1";const moderation=url.searchParams.get("moderation")==="1";if(moderation){if(!me.isRecruiter)return NextResponse.json({error:"Admin access required."},{status:403});const rows=await db`SELECT m.id,m.thread_id,m.sender_id,su.username AS sender_username,tu.username AS recipient_username,m.body,m.moderation_status,m.moderation_reason,m.deleted_at,du.username AS deleted_by_username,m.created_at FROM dm_messages m JOIN users su ON su.id=m.sender_id JOIN dm_threads t ON t.id=m.thread_id JOIN users tu ON tu.id=CASE WHEN t.user_a=m.sender_id THEN t.user_b ELSE t.user_a END LEFT JOIN users du ON du.id=m.deleted_by WHERE m.moderation_status IN ('flagged','deleted') ORDER BY m.created_at DESC LIMIT 500`;return NextResponse.json({messages:rows})}if(requests){if(!me.isRecruiter)return NextResponse.json({error:"Forbidden."},{status:403});const rows=await db`SELECT t.id AS thread_id,t.requested_at,u.id AS user_id,u.username,m.body,m.created_at FROM dm_threads t JOIN users u ON u.id=CASE WHEN t.user_a=${me.id} THEN t.user_b ELSE t.user_a END JOIN LATERAL(SELECT body,created_at FROM dm_messages WHERE thread_id=t.id ORDER BY created_at ASC LIMIT 1)m ON TRUE WHERE(t.user_a=${me.id} OR t.user_b=${me.id}) AND t.approval_status='pending' ORDER BY t.requested_at DESC`;return NextResponse.json({requests:rows.map(r=>({threadId:r.thread_id,userId:r.user_id,username:r.username,body:r.body,requestedAt:r.requested_at,createdAt:r.created_at}))})}if(directory){const rows=await db`SELECT u.id,u.username,COALESCE(rs.status,'none') AS status,rs.team_id,EXISTS(SELECT 1 FROM attempts a WHERE a.user_id=u.id) AS has_test,EXISTS(SELECT 1 FROM team_memberships tm WHERE tm.user_id=u.id AND tm.role='recruiter') AS is_recruiter FROM users u LEFT JOIN recruitment_status rs ON rs.user_id=u.id WHERE u.id<>${me.id} ORDER BY LOWER(u.username) ASC LIMIT 500`;const people=rows.map(r=>({id:String(r.id),username:String(r.username),status:r.status,teamId:r.team_id??null,hasTest:Boolean(r.has_test),isRecruiter:Boolean(r.is_recruiter)||isAdmin(String(r.username))}));return NextResponse.json({users:people.filter(person=>person.isRecruiter||person.hasTest).filter(person=>canCommunicate(me,person)||canCommunicate(person,me))})}if(targetId){const target=await getPerson(db,targetId);if(!target)return NextResponse.json({error:"Player not found."},{status:404});if(!target.isRecruiter&&!(await hasTest(db,target.id)))return NextResponse.json({error:"That player has not unlocked messages yet."},{status:403});if(!canCommunicate(me,target)&&!canCommunicate(target,me))return NextResponse.json({error:"You cannot message this player from your current status."},{status:403});const thread=await getThread(db,me.id,target.id);if(!thread)return NextResponse.json({thread:null,messages:[],target});const messages=await db`SELECT m.id,m.sender_id,u.username AS sender_username,m.body,m.moderation_status,m.moderation_reason,m.deleted_at,du.username AS deleted_by_username,m.created_at FROM dm_messages m JOIN users u ON u.id=m.sender_id LEFT JOIN users du ON du.id=m.deleted_by WHERE m.thread_id=${thread.id} ORDER BY m.created_at ASC`;return NextResponse.json({thread:{id:thread.id,approvalStatus:thread.approval_status,requestedAt:thread.requested_at,approvedAt:thread.approved_at},messages,target})}const rows=await db`SELECT t.id AS thread_id,t.approval_status,t.updated_at,u.id AS user_id,u.username,m.body AS last_body,m.created_at AS last_created_at FROM dm_threads t JOIN users u ON u.id=CASE WHEN t.user_a=${me.id} THEN t.user_b ELSE t.user_a END LEFT JOIN LATERAL(SELECT body,created_at FROM dm_messages WHERE thread_id=t.id ORDER BY created_at DESC LIMIT 1)m ON TRUE WHERE t.user_a=${me.id} OR t.user_b=${me.id} ORDER BY COALESCE(m.created_at,t.updated_at) DESC`;return NextResponse.json({me,conversations:rows.map(r=>({threadId:r.thread_id,approvalStatus:r.approval_status,userId:r.user_id,username:r.username,lastBody:r.last_body,lastCreatedAt:r.last_created_at}))})}catch(error){console.error("Messages load failed",error);return NextResponse.json({error:"Unable to load messages."},{status:500})}}

export async function POST(request:Request){try{await ensureSchema();const user=await getCurrentUser();if(!user)return NextResponse.json({error:"Not authenticated."},{status:401});const db=getDb();const me=await getPerson(db,user.id);if(!me)return NextResponse.json({error:"Profile not found."},{status:404});if(!me.isRecruiter&&!(await hasTest(db,me.id)))return NextResponse.json({error:"Take the assessment at least once to unlock messages."},{status:403});const body=await request.json();const targetId=String(body.userId??"");const message=String(body.message??"").trim().slice(0,MAX_BODY);if(!targetId||!message)return NextResponse.json({error:"A recipient and message are required."},{status:400});if(targetId===me.id)return NextResponse.json({error:"You cannot message yourself."},{status:400});const target=await getPerson(db,targetId);if(!target)return NextResponse.json({error:"Player not found."},{status:404});if(!target.isRecruiter&&!(await hasTest(db,target.id)))return NextResponse.json({error:"That player has not unlocked messages yet."},{status:403});if(!canCommunicate(me,target))return NextResponse.json({error:"You cannot message this player."},{status:403});const[userA,userB]=pair(me.id,target.id);let thread=await getThread(db,me.id,target.id);const requiresApproval=shouldRequireApproval(me,target);const moderationResult=moderateMessage(message);if(!thread){const status=requiresApproval?"pending":"active";const rows=await db`INSERT INTO dm_threads(user_a,user_b,approval_status,requested_at,last_request_at,updated_at) VALUES(${userA},${userB},${status},${requiresApproval?new Date().toISOString():null},${requiresApproval?new Date().toISOString():null},NOW()) RETURNING id,approval_status,requested_at,approved_at,last_request_at`;thread=rows[0]}else if(thread.approval_status==='rejected'&&requiresApproval){const last=thread.last_request_at?new Date(thread.last_request_at).getTime():0;const remaining=REQUEST_COOLDOWN_MS-(Date.now()-last);if(remaining>0)return NextResponse.json({error:`Please wait ${Math.ceil(remaining/1000)} seconds before sending another request.`},{status:429});await db`UPDATE dm_threads SET approval_status='pending',requested_at=NOW(),last_request_at=NOW(),updated_at=NOW() WHERE id=${thread.id}`;thread={...thread,approval_status:'pending'}}else if(requiresApproval&&thread.approval_status==='pending')return NextResponse.json({error:"Your first message is waiting for the recruiter to approve it."},{status:409});else if(!requiresApproval&&thread.approval_status==='rejected')await db`UPDATE dm_threads SET approval_status='active',updated_at=NOW() WHERE id=${thread.id}`;if(requiresApproval&&thread.approval_status==='pending'){const existing=await db`SELECT id FROM dm_messages WHERE thread_id=${thread.id} ORDER BY created_at ASC LIMIT 1`;if(existing.length)return NextResponse.json({error:"Your first message is already waiting for approval."},{status:409})}const saved=await db`INSERT INTO dm_messages(thread_id,sender_id,body,moderation_status,moderation_reason) VALUES(${thread.id},${me.id},${message},${moderationResult.status},${moderationResult.reason}) RETURNING id,sender_id,body,moderation_status,moderation_reason,created_at`;await db`UPDATE dm_threads SET updated_at=NOW() WHERE id=${thread.id}`;return NextResponse.json({ok:true,threadId:thread.id,approvalStatus:requiresApproval?"pending":"active",moderation:moderationResult.status==='flagged'?{flagged:true,reason:moderationResult.reason,message:"This message was flagged for potentially inappropriate language."}:{flagged:false},message:saved[0]})}catch(error){console.error("Message send failed",error);return NextResponse.json({error:"Unable to send message."},{status:500})}}

export async function PATCH(request:Request){try{await ensureSchema();const user=await getCurrentUser();if(!user)return NextResponse.json({error:"Not authenticated."},{status:401});const db=getDb();const me=await getPerson(db,user.id);if(!me?.isRecruiter)return NextResponse.json({error:"Admin/recruiter access required."},{status:403});const body=await request.json();const threadId=String(body.threadId??"");const action=String(body.action??"");const messageId=String(body.messageId??"");if(action==='delete_message'){if(!messageId)return NextResponse.json({error:"Message ID required."},{status:400});const rows=await db`SELECT id FROM dm_messages WHERE id=${messageId} LIMIT 1`;if(!rows.length)return NextResponse.json({error:"Message not found."},{status:404});await db`UPDATE dm_messages SET body='',moderation_status='deleted',moderation_reason=COALESCE(moderation_reason,'Deleted by administrator'),deleted_by=${me.id},deleted_at=NOW() WHERE id=${messageId}`;return NextResponse.json({ok:true,messageId,deletedBy:me.username})}if(!threadId||!["approve","reject"].includes(action))return NextResponse.json({error:"Invalid request."},{status:400});const rows=await db`SELECT id,user_a,user_b,approval_status FROM dm_threads WHERE id=${threadId} AND(user_a=${me.id} OR user_b=${me.id}) LIMIT 1`;if(!rows.length||rows[0].approval_status!=='pending')return NextResponse.json({error:"Message request not found."},{status:404});const status=action==='approve'?'active':'rejected';await db`UPDATE dm_threads SET approval_status=${status},approved_at=${action==='approve'?new Date().toISOString():null},updated_at=NOW() WHERE id=${threadId}`;return NextResponse.json({ok:true,approvalStatus:status})}catch(error){console.error("Message moderation update failed",error);return NextResponse.json({error:"Unable to update message moderation."},{status:500})}}
