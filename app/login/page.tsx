"use client";
import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import Link from "next/link";
export default function LoginPage(){const router=useRouter();const[username,setUsername]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");setLoading(true);try{const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username,password})});const d=await r.json();if(!r.ok){setError(d.error??"Unable to sign in.");return}router.push("/dashboard");router.refresh()}catch{setError("Unable to reach the arena network.")}finally{setLoading(false)}}return <main className="arena-login"><div className="login-orbit orbit-one"/><div className="login-orbit orbit-two"/><section className="login-pitch"><div className="login-wordmark"><span className="arena-mark">CX</span><div><b>CYBER ARENA</b><small>PLAYER INTELLIGENCE NETWORK</small></div></div><div className="login-copy"><span className="login-eyebrow">01 / NETWORK ACCESS</span><h1>YOUR GAME.<br/><em>MEASURED.</em></h1><p>A competitive intelligence platform for players, recruiters and teams. Build your profile through scenario testing, performance analytics and real recruiting conversations.</p><div className="login-metrics"><div><strong>06</strong><span>IQ DOMAINS</span></div><div><strong>30+</strong><span>LIVE SCENARIOS</span></div><div><strong>24/7</strong><span>PLAYER NETWORK</span></div></div></div></section><section className="login-panel"><div className="login-panel-top"><span>SECURE CHANNEL</span><span className="login-live"><i/>ONLINE</span></div><div className="login-panel-body"><div className="login-avatar">CX</div><span className="login-eyebrow">PLAYER AUTHENTICATION</span><h2>ENTER THE<br/>NETWORK</h2><p>Access your player command center.</p><form onSubmit={submit}><label>USERNAME<input value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username" required placeholder="your username"/></label><label>PASSWORD<input value={password} onChange={e=>setPassword(e.target.value)} type="password" autoComplete="current-password" required placeholder="••••••••"/></label>{error&&<div className="login-error">{error}</div>}<button disabled={loading}>{loading?"AUTHENTICATING...":"INITIALIZE SESSION"}<span>↗</span></button></form><div className="login-divider"><span>NEW TO THE NETWORK?</span></div><Link href="/signup" className="login-create">Create your player identity <span>→</span></Link></div><div className="login-panel-foot">ENCRYPTED PLAYER DATA <span/> CYBER ARENA NETWORK</div></section></main>}
